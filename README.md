# Calculator-APP-using-Stack
Using stacks to add/subtract large real numbers

It's an application of using stack archticture to add and substract (assume the first number is > or = the second number) real numbers only.

The code doesn't handle negative numbers.

the input is some thing like that: 12.3+0.36 or 15.2687-0.036
